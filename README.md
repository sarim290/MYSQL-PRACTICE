
# MySQL Practice 🗄️

This repository contains my **MySQL learning and practice work**.
I am using this repo to build a strong foundation in SQL and practice database concepts through hands-on queries and small projects.

## 📚 Topics Covered

* Database & Table Creation
* `CREATE DATABASE`
* `CREATE TABLE`
* `ALTER TABLE`
* `DROP`
* `INSERT`
* `UPDATE`
* `DELETE`
* `SELECT`
* `WHERE`
* `DISTINCT`
* `ORDER BY`
* `LIMIT`
* `OFFSET`
* Aggregate Functions

  * `COUNT()`
  * `SUM()`
  * `AVG()`
  * `MIN()`
  * `MAX()`
* SQL Operators
* `GROUP BY`
* `HAVING`
* `INNER JOIN`
* Primary Keys
* Foreign Keys
* Constraints
* Functional Dependencies
* Normalization

  * 1NF
  * 2NF
  * 3NF
* Transactions
* Locks & Concurrency
* Deadlocks

## 📂 Repository Structure

```text
mysql-practice/
│
├── basics/
│   ├── create_database.sql
│   ├── create_table.sql
│   ├── insert.sql
│   ├── update.sql
│   └── delete.sql
│
├── queries/
│   ├── select.sql
│   ├── where.sql
│   ├── order_by.sql
│   ├── aggregate_functions.sql
│   └── joins.sql
│
├── database_design/
│   ├── functional_dependency.sql
│   └── normalization.sql
│
└── projects/
    └── ...
```

## 🎯 Purpose

The main purpose of this repository is to:

* Practice SQL regularly
* Understand how relational databases work
* Improve problem-solving skills
* Build practical database projects
* Prepare for internships and software engineering roles

## 🛠️ Tools

* **MySQL**
* **MySQL Workbench**
* **VS Code**

## 💻 Example Query

```sql
SELECT department, COUNT(*) AS total_students
FROM students
GROUP BY department
ORDER BY total_students DESC;
```

## 🚀 Learning Progress

*  SQL Basics
*  DDL & DML
*  Basic Queries
*  Filtering & Sorting
*  Aggregate Functions
*  Joins
*  More Real-World Projects

## 📌 Projects

More practical MySQL projects will be added as I continue learning and practicing.

---

### 👨‍💻 About

**Software Engineering Student | Python Developer | SQL & Databases | OOP | Workflow Automation**

This repository represents my ongoing journey of learning SQL and building practical database skills.
